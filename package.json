{
  "name": "setlist",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:pdf": "mkdir -p public/pdf && for file in chordpro/*.cho; do chordpro \"$file\" -G -o public/pdf/$(basename \"$file\" .cho).pdf; done",
    "gen:html": "mkdir -p public/html && for file in chordpro/*.cho; do chordpro \"$file\" -o public/html/$(basename \"$file\" .cho).html; done",
    "gen:setlist": "node scripts/gen-setlist.cjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
